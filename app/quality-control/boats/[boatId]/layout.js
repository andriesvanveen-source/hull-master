export const viewport = {
  width: 1100,
  minimumScale: 0.25,
  maximumScale: 5,
  userScalable: true,
  themeColor: "#071b2e"
};

export default function QualityBoatLayout({ children }) {
  return children;
}
